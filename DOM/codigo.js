var ruta = window.location;
/*un objeto (window) que tiene un atributo (location) su 
valor se guarda en la variable ruta*/
console.log(ruta);
/*mensaje en la cosola, arreglar error dentro del codigo*/
document.write("Te encuentras en: " + ruta); 
/*document es un objeto y write es una funcion de document, 
    esto se llama metodo*/
